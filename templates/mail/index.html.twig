{% extends 'mail/templateMail.html.twig' %}

{% block body %}
<div class="ligth">
    <h1>Un nouvel article a été créé</h1>
    <h2>Voici son contenu : </h2>
    <h3> {{ article.title }}</h3>
    <p>{{ article.content }}</p>
    <a href="{{ url ('article_show', {'slug': article.slug}) }}">Lire l'article</a>
</div>

{% endblock %}




